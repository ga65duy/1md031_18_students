/*
var vm = new Vue({
    el: '#myID',
    data: {
        arbitraryVariableName: 'Välj en burgare'
    }
})
<ol>
<li v-for="burger in burgers" v-if="burger.stock > 0">
    {{ burger.name }}
</li>
</ol>*/
